# Copy this file to .env and fill in your values.
# .env is git-ignored and must never be committed.

# Azure AD tenant ID
TENANT_ID=XX

# App registration (the client calling the API)
CLIENT_ID=XX

# Client secret – rotate the old one if it was ever committed to source control
CLIENT_SECRET=your-client-secret-here

API_SCOPE=client_id/.default
